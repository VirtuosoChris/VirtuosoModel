#ifndef VIRTUOSO_MESH_IO_H_INCLUDED
#define VIRTUOSO_MESH_IO_H_INCLUDED

#include "Mesh.h"
#include "GPUMesh.h"

namespace Virtuoso{

namespace VBO{

   /* Mesh loadMesh(std::istream& in);
    GPUMesh loadMesh(std::istream& in);

    Mesh writeMesh(std::ostream& out);
    GPUMesh writeMesh(std::ostream& out);
*/
    }
}


#endif
